Process for replicating:

run the requirements file
`pip install -e requirements.txt`

run through each of the notebooks in scripts in order, take note of the tile2net query notebook which has special instructions
between 4 and 5, there is cleaning that needs to be done for the tile2net output.
